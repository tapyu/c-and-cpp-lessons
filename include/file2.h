#ifndef FILE2_H
#define FILE2_H

void increment_file2(void); // it is implicitly treated as a `extern`. You can put the `extern` keyword without any problem for the sake of clarity. Although it is also correct, it is less common. Therefore the `extern` keyword is commonly used for global variables only

#endif